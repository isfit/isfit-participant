<%= render 'menu' %>

<table class="table table-striped">
  <tr>
    <th>Title</th>
    <th>User</th>
    <th>Information category</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @information_pages.each do |information_page| %>
  <tr>
    <td><%= information_page.title %></td>
    <td><%= information_page.user.name %></td>
    <td><%= information_page.information_category.title %></td>
    <td><%= link_to image_tag('icons/report_go.png') + ' Show', information_page %></td>
    <td><%= link_to image_tag('icons/report_edit.png') + ' Edit', edit_information_page_path(information_page) %></td>
    <td><%= link_to image_tag('icons/report_delete.png') + ' Delete', information_page, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<%= render 'information' %>

