<%= form_for(@information_page) do |f| %>
  <% if @information_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@information_page.errors.count, "error") %> prohibited this information_page from being saved:</h2>

      <ul>
      <% @information_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
    <%= f.hidden_field :user_id, :value => current_user.id %>
  <div class="field">
    <%= f.label :information_category_id %><br />
    <%= f.text_field :information_category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
