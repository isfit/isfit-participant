<h1>Listing information categories</h1>

<table class="full">
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @information_categories.each do |information_category| %>
  <tr>
    <td><%= information_category.title %></td>
    <td><%= link_to 'Show', information_category %></td>
    <td><%= link_to 'Edit', edit_information_category_path(information_category) %></td>
    <td><%= link_to 'Destroy', information_category, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Information category', new_information_category_path %> | <%= link_to "Information pages", information_pages_path %>
